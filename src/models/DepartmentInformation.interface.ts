export interface DepartmentInformation {
  departmentId?: {
    correlation: string;
  };
  departmentTypeId?: {
    correlation: string;
  };
  sectionId?: number;
  payCodeId?: {
    correlation: string;
  };
  budgetCode?: string;
}
